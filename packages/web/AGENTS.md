# AGENTS.md — packages/web

## Purpose

- SPA UI for Append (React + TanStack Router), deployed to Cloudflare Pages; talks to the API worker.

## Commands

- `pnpm dev` (Vite dev server)
- `pnpm build` (production build)
- `pnpm typecheck`
