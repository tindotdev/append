# AGENTS.md — packages/ai-gateway-lab

## Purpose

- Local lab for testing Cloudflare AI Gateway + LLM providers
- Use Vercel `ai` sdk

## Commands

- `pnpm lab` (run the lab script; requires .env)
- `pnpm typecheck`
