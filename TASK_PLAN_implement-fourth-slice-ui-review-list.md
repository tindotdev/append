# TASK_PLAN_implement-fourth-slice-ui-review-list

## Requirements
- [ ] `GET /api/batch/:id` returns the Step 4 batch read-model with each candidate containing exactly the fields and ordering specified in `docs/vertical-slice.md` Step 4. -> `packages/api/src/routes/batch.ts` / `packages/api/test/batch.spec.ts` -> `pnpm --filter append-api test`
- [ ] `PUT /api/candidate/:id` supports per-candidate Save and Clear overrides with validation and optimistic-locking conflict detection (`409 VERSION_CONFLICT` with `details.currentVersion`). -> `packages/api/src/routes/candidate.ts` (`candidateRoutes`) / `packages/api/src/index.ts` (`app.route("/api/candidate", candidateRoutes)`) / `packages/api/test/candidate.spec.ts` -> `pnpm --filter append-api test`
- [ ] `/batch/:id` renders suggested bucket/text read-only, renders editable "final" bucket/text inputs, and provides explicit `Save` and `Clear overrides` actions with no auto-save. -> `packages/web/src/pages/BatchDetailPage.tsx` -> `pnpm --filter append-web build` + `pnpm dev` (manual UI sanity checklist in plan)
- [ ] SPA routing remains TanStack Router; API remains Hono on Cloudflare Workers (`docs/design.md`, `docs/build-plan.md`). -> `packages/web/src/main.tsx` (router) / `packages/api/src/index.ts` (Hono app) -> `pnpm typecheck` + `pnpm --filter append-web build`
- [ ] All `/api/*` routes require a valid session cookie; otherwise return `401 UNAUTHORIZED` (`docs/vertical-slice.md`). -> `packages/api/src/index.ts` (auth middleware) / `packages/api/test/batch.spec.ts` / `packages/api/test/candidate.spec.ts` -> `pnpm --filter append-api test`
- [ ] `/api/*` error responses use `{ "error": { "code": string, "message": string }, "details"?: object }` (`docs/vertical-slice.md`). -> `packages/api/src/lib/api-error.ts` -> `pnpm --filter append-api test`
- [ ] Allowed bucket slugs are exactly `foundations | backend | frontend | dx-tooling | deep-concepts` (`docs/vertical-slice.md`, `docs/design.md`). -> `packages/api/src/db/domain.schema.ts` / `packages/api/src/lib/suggestions.ts` / `packages/web/src/pages/BatchDetailPage.tsx` -> `pnpm --filter append-api test` + `pnpm typecheck`
- [ ] Candidate edits use atomic conditional updates with `expectedVersion` to prevent TOCTOU races (`docs/design.md`, `docs/vertical-slice.md`). -> `packages/api/src/routes/candidate.ts` (single conditional `UPDATE ... WHERE id = :id AND version = :expectedVersion`) -> `pnpm --filter append-api test` (version conflict + increment assertions)
- [ ] No auto-save; all persistence is behind explicit user actions (`docs/vertical-slice.md`). -> `packages/web/src/pages/BatchDetailPage.tsx` (Save/Clear handlers only; no onChange persistence) -> `pnpm --filter append-web build` + `pnpm dev` (manual UI sanity checklist in plan)
- [ ] Loading spinner shown while batch is fetching -> `packages/web/src/pages/BatchDetailPage.tsx` -> `pnpm --filter append-web build` + `pnpm dev` (manual)
- [ ] 403/404/5xx states display the specified messages -> `packages/web/src/pages/BatchDetailPage.tsx` -> `pnpm --filter append-web build` + `pnpm dev` (manual)
- [ ] Each candidate row displays term, normalized term (if different), suggested bucket/text (read-only) -> `packages/web/src/pages/BatchDetailPage.tsx` -> `pnpm --filter append-web build` + `pnpm dev` (manual)
- [ ] Bucket dropdown contains exactly: `foundations`, `backend`, `frontend`, `dx-tooling`, `deep-concepts` -> `packages/web/src/pages/BatchDetailPage.tsx` -> `pnpm --filter append-web build` + `pnpm dev` (manual)
- [ ] Text input accepts single-line text up to 500 characters -> `packages/web/src/pages/BatchDetailPage.tsx` / `packages/api/src/routes/candidate.ts` (validation) -> `pnpm --filter append-api test` (chosenText validation) + `pnpm dev` (manual)
- [ ] Save button calls PUT with current draft values and `expectedVersion` -> `packages/web/src/pages/BatchDetailPage.tsx` / `packages/web/src/lib/api.ts` -> `pnpm --filter append-web build` + `pnpm dev` (manual)
- [ ] Clear button calls PUT with `chosenBucket: null, chosenText: null` -> `packages/web/src/pages/BatchDetailPage.tsx` / `packages/api/src/routes/candidate.ts` -> `pnpm --filter append-api test` (clear overrides) + `pnpm dev` (manual)
- [ ] 409 conflict triggers batch re-fetch and displays toast -> `packages/web/src/pages/BatchDetailPage.tsx` (409 handler + toast) -> `pnpm --filter append-api test` (409 response) + `pnpm dev` (manual multi-tab)
- [ ] Row inputs are disabled during save -> `packages/web/src/pages/BatchDetailPage.tsx` -> `pnpm --filter append-web build` + `pnpm dev` (manual)
- [ ] Missing/in-progress/error suggestion states display correct badges -> `packages/web/src/pages/BatchDetailPage.tsx` -> `pnpm --filter append-web build` + `pnpm dev` (manual)
- [ ] Suggestion error tooltip shows `suggestionError` text -> `packages/web/src/pages/BatchDetailPage.tsx` -> `pnpm --filter append-web build` + `pnpm dev` (manual)
- [ ] Align web/API types to the Step 4 read-model — update `packages/web/src/lib/api.ts` so `Candidate` and `BatchResponse` match the Step 4 `GET /api/batch/:id` shape exactly (including suggestion + chosen + version/materialization fields). -> `packages/web/src/lib/api.ts` (`Candidate`, `BatchResponse`) -> `pnpm typecheck`
- [ ] Implement `PUT /api/candidate/:id` in the Worker — add `packages/api/src/routes/candidate.ts`, export `candidateRoutes`, and register it in `packages/api/src/index.ts` via `app.route("/api/candidate", candidateRoutes)`; implement the handler flow: -> `packages/api/src/routes/candidate.ts` / `packages/api/src/index.ts` -> `pnpm --filter append-api test`
- [ ] Update the `/batch/:id` UI to be a real review list — modify `packages/web/src/pages/BatchDetailPage.tsx` to: -> `packages/web/src/pages/BatchDetailPage.tsx` -> `pnpm --filter append-web build` + `pnpm dev` (manual UI sanity checklist in plan)
- [ ] Add API integration tests for Step 4 writes and tighten Step 4 reads — add `packages/api/test/candidate.spec.ts` and extend `packages/api/test/batch.spec.ts`: -> `packages/api/test/candidate.spec.ts` / `packages/api/test/batch.spec.ts` -> `pnpm --filter append-api test`
- [ ] `PUT /api/candidate/:id` tests: `401` unauthenticated; `404` missing; `403` non-owner; `400` missing `expectedVersion`; `400` missing both patch keys; `400` invalid bucket; `400` chosenText empty; `400` chosenText contains `\n`; `400` chosenText > 500 chars; `409` version conflict returns `details.currentVersion`; `200` updates fields and increments `version`. -> `packages/api/test/candidate.spec.ts` -> `pnpm --filter append-api test`
- [ ] `GET /api/batch/:id` test: assert each candidate includes the Step 4 fields (present and `null` where applicable) in addition to existing assertions. -> `packages/api/test/batch.spec.ts` -> `pnpm --filter append-api test`
- [ ] Remove contract drift — update the inline doc comment in `packages/api/src/routes/batch.ts` for `GET /api/batch/:id` to match the Step 4 response shape, and flip Step 4 to `[x]` in `docs/vertical-slice.md` after implementation and tests pass. -> `packages/api/src/routes/batch.ts` / `docs/vertical-slice.md` -> `pnpm --filter append-api test` + `pnpm typecheck` + `pnpm --filter append-web build`

## Blockers
- None.
