{
	"name": "append",
	"version": "1.0.0",
	"private": true,
	"type": "module",
	"scripts": {
		"setup:deps": "pnpm install && pnpm --filter @append/api db:migrate:local",
		"dev:nosecrets": "pnpm -r --parallel dev",
		"dev:api": "pnpm --filter @append/api run dev",
		"dev:web": "pnpm --filter @append/web run dev",
		"dev:extension": "pnpm --filter @append/extension dev",
		"build": "pnpm -r --if-present build",
		"docs:policy": "node scripts/check-docs-policy.mjs",
		"typecheck": "pnpm -r --if-present typecheck",
		"lint": "biome lint --write .",
		"format": "biome format --write .",
		"check": "biome check --write .",
		"check:unsafe": "biome check --write --unsafe .",
		"ci:lint": "biome ci .",
		"lint:boundaries": "pnpm --filter @append/web --filter @append/api lint:boundaries",
		"test": "pnpm -r --if-present test",
		"test:api": "pnpm --filter @append/api test",
		"test:watch": "pnpm -r --if-present --parallel test:watch",
		"ci": "pnpm lint:boundaries && pnpm docs:policy && pnpm ci:lint && pnpm typecheck && pnpm test && pnpm --filter @append/api test:extension",
		"ci:all": "pnpm ci && pnpm --filter @append/api test:preview",
		"ci:fast": "pnpm lint:boundaries && pnpm docs:policy && pnpm typecheck",
		"deploy:api": "pnpm --filter @append/api run deploy",
		"deploy:web": "pnpm --filter @append/web run deploy",
		"deploy": "pnpm deploy:api && pnpm deploy:web",
		"lab": "pnpm --filter @append/ai-gateway-lab lab",
		"prepare": "prek install",
		"cf-typegen": "pnpm -r --if-present cf-typegen"
	},
	"license": "MIT",
	"packageManager": "pnpm@10.28.2",
	"volta": {
		"node": "24.13.0"
	},
	"engines": {
		"node": ">=20",
		"pnpm": ">=9"
	},
	"pnpm": {
		"peerDependencyRules": {
			"ignoreMissing": [
				"vitest"
			],
			"allowedVersions": {
				"drizzle-orm": ">=0.41.0"
			}
		},
		"overrides": {
			"vitest": "~3.2.4",
			"@vitest/runner": "~3.2.4",
			"@vitest/snapshot": "~3.2.4",
			"seroval": ">=1.4.1",
			"esbuild": ">=0.25.0"
		}
	},
	"devDependencies": {
		"@biomejs/biome": "^2.3.10",
		"@typescript-eslint/eslint-plugin": "^8.50.1",
		"@typescript-eslint/parser": "^8.50.1",
		"eslint": "^9.39.2",
		"eslint-import-resolver-typescript": "^4.4.4",
		"eslint-plugin-boundaries": "^5.3.1"
	}
}
